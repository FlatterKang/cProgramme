/*
    输入一个整数，连续显示出该整数个 *
*/
#include <stdio.h>

int main() {
    int num;

    printf("请输入一个正数：");
    scanf("%d", &num);

    while (no-- > 0)
        putchar('*');
    putchar('\n');

    return 0;
}